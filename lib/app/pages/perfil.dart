import 'package:flutter/material.dart';

class Perfil extends StatelessWidget{
  Widget build( BuildContext){
    return AppBar(
      title: Container(
        child: Text('Perfil'),
      ),
    );
  }
}